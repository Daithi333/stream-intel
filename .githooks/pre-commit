#!/bin/sh

# Go checks
unformatted=$(gofmt -l .)
if [ -n "$unformatted" ]; then
  echo "gofmt check failed. Run 'gofmt -w .' on:"
  echo "$unformatted"
  exit 1
fi

go vet ./...

# UI checks
if [ -d "ui" ]; then
  cd ui
  pnpm run lint || exit 1
  pnpm run build || exit 1
  cd ..
fi
